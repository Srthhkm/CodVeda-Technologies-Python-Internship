# To-Do List Application

A simple command-line based To-Do List application built using Python.

## Features
- Add new tasks
- View all tasks
- Mark tasks as completed
- Delete tasks
- Persistent storage using JSON file

## How to Run
1. Open the folder containing `todo.py`
2. Run the following command in terminal or CMD:

```bash
python todo.py
