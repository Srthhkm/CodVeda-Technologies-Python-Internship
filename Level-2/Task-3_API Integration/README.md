# API Integration – Weather App

A Python script that fetches real-time weather data using an external API.

## Features
- Fetches live weather data for any city
- Displays temperature, weather condition, and humidity
- Uses external API with JSON response
- Handles request errors gracefully

## How to Run
1. Open the folder containing `api_integrator.py`
2. Run the following command in terminal or CMD:

```bash
python api_integrator.py 
